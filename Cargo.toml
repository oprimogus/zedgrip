[package]
name = "zedgrip"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = { git = "https://github.com/zed-industries/zed", package = "gpui", default-features = false, features = ["winit", "wgpu", "x11"] }
tokio = { version = "1.37.0", features = ["full"] }
